-- Виджеты на главной странице убраны из продукта (см. план правок,
-- п. 10): секцию заменили порядком "Новинки → Категории", отдельного
-- блока с виджетами больше нет ни на сайте, ни в админке.
--
-- Перед применением на боевой базе: если в таблице widgets есть значения,
-- которые жалко потерять (например, картинка баннера), перенесите их
-- вручную в settings.hero_image до накатки этой миграции — после
-- "drop table" данные будут удалены безвозвратно.

drop table if exists widgets;
